<odoo>
    <record id="purchase_order_view_form_inherit_vendorguard" model="ir.ui.view">
        <field name="name">purchase.order.form.inherit.vendorguard</field>
        <field name="model">purchase.order</field>
        <field name="inherit_id" ref="purchase.purchase_order_form"/>
        <field name="arch" type="xml">
            <xpath expr="//page[@name='products']" position="after">
                <page string="Fraud Flags" name="vendorguard_fraud_flags">
                    <field name="fraud_flag_ids" readonly="1">
                        <list decoration-danger="severity in ('critical','high')"
                              decoration-warning="severity == 'medium'">
                            <field name="create_date"/>
                            <field name="flag_type"/>
                            <field name="severity"/>
                            <field name="amount"/>
                            <field name="state"/>
                        </list>
                    </field>
                </page>
            </xpath>
        </field>
    </record>
</odoo>
